apiVersion: v1
kind: Service
metadata:
  name: preview-demo
  namespace: preview-demo
spec:
  selector:
    app: preview-demo
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
